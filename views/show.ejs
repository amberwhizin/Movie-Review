<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include ('../partials/head.ejs') %>
    <title></title>
  </head>
  <body>
    <div class="container">
  <nav>
  <ul class="nav">
    <li class="nav-item">
      <a class="nav-link active" href="/critiques"><button class="btn btn-primary">Home</button></a>
    </li>
  </nav>
      <!-- define title -->
      <% const title = critique.title %>    
      <h2><%= title.toUpperCase()[0] + title.slice(1) %></h2>
      <!-- rating -->
      Rating: <%= critique.rating %>  
      <!-- image will be-->
      <div class="show-image">
        <img src="<%= critique.img %>" class="rounded"/><br />
         <p><%= critique.entry %></p>
            <!-- edit -->
         <a href="/critiques/<%= critique.id %>/edit">Edit</a><br />
              <!-- delete -->
      <form action="/critiques/<%= critique.id %>?_method=DELETE" method="POST">
        <button type="submit" value="DELETE" class="btn btn-danger">Delete</button>
     </form>
      
        <!-- review entry -->
        <!-- i don't want the user to be able to enter anything on this page, thats what the edit page is for, this is for a close up -->

    
      </div>
      <br />
    </div>
  
      <%- include ('../partials/script.ejs') %>
  
  </body>
</html>
